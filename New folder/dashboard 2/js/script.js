/* ================================================================
   ESSARA Dashboard — script.js
   Handles: nav tab switching, theme toggle, currency switching
   ================================================================ */

document.addEventListener('DOMContentLoaded', () => {

  // 1. NAV TAB SWITCHING
  // Removed: Tabs are now actual links to separate pages.


  // ============================
  // 2. THEME TOGGLE (Light/Dark)
  // ============================
  // Handled by the shared ../js/script.js (with _essaraThemeBound guard).
  // No duplicate handler here to avoid double-toggle.


  // ============================
  // 3. CURRENCY SWITCHING
  // ============================
  // Currency switching is now handled by the central data engine in ../js/script.js
});
